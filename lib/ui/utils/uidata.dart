import 'package:flutter/material.dart';

class Uidata {
  static const Color primaryColor = Colors.brown;
  // static const Color accentColor = Color.fromRGBO(51, 158, 102, 0.5);
  static const Color accentColor = Color(0xA5C6BB);

  static const Color btnColor = Color.fromRGBO(181,101,29, 1);
}